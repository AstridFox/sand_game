{
  "name": "web-demo",
  "version": "0.1.0",
  "description": "Web demo application composing the engine, behaviors, and UI renderer",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "behavior-library": "^0.1.0",
    "simulation-engine": "^0.1.0",
    "ui-renderer": "^0.1.0"
  }
}