{
  "name": "simulation-engine",
  "version": "0.1.0",
  "description": "Grid, scheduler, and simulation loop for the falling sand simulation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest"
  },
  "devDependencies": {
    "fast-check": "^3.0.0"
  }
}